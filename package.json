{
  "name": "fanyi-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Open-source immersive and selection translation browser extension",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "latest",
    "@types/node": "^22.20.3",
    "esbuild": "latest",
    "playwright-core": "^1.63.0",
    "typescript": "latest",
    "vitest": "latest"
  }
}
