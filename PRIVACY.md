# fanyi 隐私政策

更新日期：2026-09-18

fanyi 是一个开源的浏览器扩展，用于翻译网页正文和选中文本。fanyi 没有账号系统，不运营任何服务器，不收集、不存储、不出售任何用户数据。

## 扩展会处理哪些数据

**待翻译的文本。** 只有你主动发起翻译时，相关文本才会离开浏览器：开启网页翻译后，是当前页面中可见的正文段落和页面标题；使用划词翻译时，是你选中的文本。这些文本会直接从你的浏览器发送到你选择的翻译服务：

- 内置的 Google 翻译公共接口（`translate.googleapis.com`），受 [Google 隐私权政策](https://policies.google.com/privacy) 约束。
- 你在设置中配置的 OpenAI 兼容接口（默认 `api.openai.com`，也可以是任何你自己填写的服务地址），受该服务提供方的隐私政策约束。

fanyi 不会经由任何中转服务器转发这些文本，也不会把它们发送到上述之外的任何地方。

**设置与 API Key。** 语言偏好、译文样式、网站排除列表以及你填写的 API Key 都保存在浏览器的本地存储（`chrome.storage.local`）中。它们不会同步到浏览器账号，也不会上传到任何地方。API Key 只会随翻译请求发送给你自己配置的那个接口。

**翻译缓存。** 已翻译的文本会在扩展后台的内存中短暂缓存，用于避免重复请求。缓存不会写入磁盘，浏览器关闭即清除。

## 扩展不会做什么

- 不收集浏览历史、访问过的网址或页面内容用于翻译以外的任何目的。
- 不包含任何统计、分析、广告或追踪代码。
- 不在未开启翻译的页面上读取或发送页面内容。
- 不将任何数据用于与翻译无关的用途，不出售或转让给第三方。

## 权限说明

| 权限 | 用途 |
| --- | --- |
| `storage` | 在本地保存设置和 API Key |
| `activeTab` | 弹窗中显示当前标签页的标题和网址，并向其发送翻译指令 |
| `contextMenus` | 提供「翻译网页」和「翻译选中文本」右键菜单 |
| `translate.googleapis.com` | 调用内置的 Google 翻译 |
| `api.openai.com` | 调用默认的 OpenAI 接口 |
| 可选的其他网址 | 仅当你把接口地址改为其他服务时，才会请求该网址的访问权限 |
| 在所有网站上运行 | 翻译功能需要在你打开的任意网页上插入译文，代码只在你发起翻译后才读取页面文本 |

## 删除数据

在设置页清空 API Key 即可删除密钥；卸载扩展会清除全部本地数据。翻译服务提供方保留的数据请依据其各自的政策处理。

## 开源与联系

fanyi 以 Apache-2.0 许可证开源，完整代码见 <https://github.com/rockdai/fanyi>，你可以审查、修改并自行构建。关于本政策的任何问题，请在 [GitHub Issues](https://github.com/rockdai/fanyi/issues) 中提出。本政策如有变更，会随扩展更新一并发布在此页面。
